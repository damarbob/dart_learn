name: dart_learn
description: A sample command-line application.
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.0

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  http: ^1.3.0

  command_runner:
    path: ../dart_command_runner_learn

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
